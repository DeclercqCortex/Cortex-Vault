// This file is unused as of Cluster 21 v1.2 ship. The Diablo 2 rune
// feature was reverted before shipping; the decorative-divider picker
// went back to its original 14-glyph set and the Special Characters
// popover dropped its rune section.
//
// Slated for deletion in the next commit (`git rm` / `Remove-Item`).
// Kept here as an empty stub so a stale `import` from a dirty branch
// doesn't break the build during the revert; nothing in the codebase
// references it anymore.

export {};
